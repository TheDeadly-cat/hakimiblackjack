# Hakimi Blackjack Lab V0.1.1 复核与补齐报告

日期：2026-09-10。范围按用户本次确认，补齐并验收 V0.1 手动记录闭环。本次已完成本地工程验收；不涉及 V0.2 数学引擎、识别、捕获或平台发布。

## 结果

**116 个测试全部通过，0 失败、0 错误、0 跳过。** 包括 7 个实际 Tk 控件回调工作流；它们已经包含在 116 中，不重复计数。核心自检、字节码编译、两个窗口尺寸的截图检查均退出 0。

项目继续位于 `C:\Users\Administrator\Desktop\Hakimi_Blackjack_Lab`。版本更新为 0.1.1，数据库结构版本为 2。当前任务目录中的空 Git 仓库未提交、未推送。

## 检查与修复

| 原问题 | 修复后行为 |
|---|---|
| events.seq 在整个库全局唯一，新会话序号从1开始且 INSERT OR IGNORE 静默丢弃 | 约束改为会话内唯一，相同ID不同内容与序号冲突报错；v1迁移前备份 |
| 非法发牌/操作先写库后重放报错，界面却称未入账 | 副本完整重放预演、SQLite事务提交、最后发布内存；失败两边都不改变 |
| 烧完牌仍可发具体牌；T与具体十点牌复用同一容量 | 总物理容量与十点组共同校验，缺口存在也不能超过最大容量 |
| 多张未知牌靠列表首张定位；揭示底牌清掉其他候选 | 用原始发牌 event_id 定位，区分规则暗牌与观察未知，只更新对应计数 |
| 部分未知手按已知点数计算 | 只要有未知牌，总点数未知，不能确定结算 |
| 分牌原手被当作未分牌手，天然BJ/DAS规则错误 | 原手和新手均有分牌来源标记；分牌21按普通21记账；两手均检查DAS |
| 停牌后仍能分/加倍，重复加倍、庄家玩家动作、BJ后动作未挡住 | 所有动作统一状态校验；未知手、关闭手和非法时机拒绝 |
| 未知BJ赔付默认3:2；未知追加注规则直接扣全额 | 需要明确所用赔付；庄家BJ追加注仅验证“全部注损失”，未知或其他规则拒绝收益输出 |
| 多次纠错覆盖前次修正，撤销新靴后身份指向错误 | 纠错顺序合成、可撤销纠错；牌靴/轮次上下文从重放恢复 |
| 界面动作按钮一直禁用、手牌选择带显示文本导致找不到手、揭示入口无效、K按钮重叠 | 修复真实控件回调、ID映射、具体暗牌选择及卡牌键布局 |
| 恢复前先创建新会话，导致空会话堆积；导入/历史纠错无入口 | 启动先检查历史；加入会话切换、历史时点、纠错、JSON/CSV导入和一致备份 |
| 窗口变小时底部被挤出，部分控件完全不显示 | 顶部/底部固定、主体伸缩；压缩座位区、增加组成滚动条；最低1180×800 |

未凭空恢复旧版曾丢弃的事件。原始用户数据库本次只读核对，未执行迁移、未写入测试数据；测试均使用临时库。用户以后启动新版时会对实际旧库生成备份再迁移。

## 原始失败与测试修订

- 原有 58 个测试本次复跑通过，原始输出为 [baseline-tests.txt](review-20260910/baseline-tests.txt)。这没有覆盖上述缺陷。
- 修改实现前新增 31 个回归测试，得到 **38 个失败断言（含子测试）**，退出 1：[regressions-before.txt](review-20260910/regressions-before.txt)。
- 首次核心修复合跑 89 个测试得到 5 failures / 4 errors，原始输出为 [intermediate-tests-1.txt](review-20260910/intermediate-tests-1.txt)；后续修复后通过，未删除失败记录。
- 三个原测试契约得到纠正：含未知牌的总点数应未知；T不能判定的是“同牌面”配对，同点值可判定；相同事件ID但不同内容应拒绝，幂等用例改为相同内容并另加冲突用例。
- 原始界面控件冒烟未发现小窗口遮挡。最初 1180×760 截图仍保留为 [失败布局](review-20260910/ui-1180x760.png)；迭代说明见 [visual-iterations.md](review-20260910/visual-iterations.md)。

## 最终真实命令与输出

执行目录：`C:\Users\Administrator\Desktop\Hakimi_Blackjack_Lab`。环境：Windows 11 / Python 3.14.6，原历史报告中的 Python 3.14.7 不作为本次环境结论。

统一入口：`python scripts/verify_release.py`，退出 0。子命令如下：

| 实际命令 | 退出码 | 完整输出 |
|---|---:|---|
| `C:\Users\Administrator\AppData\Local\Python\pythoncore-3.14-64\python.exe -m unittest discover -s tests -v` | 0 | [final-tests.txt](review-20260910/final-tests.txt) |
| `C:\Users\Administrator\AppData\Local\Python\pythoncore-3.14-64\python.exe -m blackjack_lab.main --check` | 0 | [final-selfcheck.txt](review-20260910/final-selfcheck.txt) |
| `C:\Users\Administrator\AppData\Local\Python\pythoncore-3.14-64\python.exe -m compileall -q blackjack_lab tests scripts` | 0 | [final-compile.txt](review-20260910/final-compile.txt) |
| `C:\Users\Administrator\AppData\Local\Python\pythoncore-3.14-64\python.exe scripts/ui_visual_check.py` | 0 | [final-ui-visual.txt](review-20260910/final-ui-visual.txt) |

最终测试尾部：

```text
Ran 116 tests in 3.348s

OK
```

自建场景截图已实际查看：

- [1360×900](review-20260910/ui-1360x900.png)
- [1180×800](review-20260910/ui-1180x800.png)

布局检查同时核对越界和未映射的关键控件。截图仅为自建演示数据、临时数据库，无真实牌桌画面。

## 备份与证据身份

原目录完整备份：`C:\Users\Administrator\Documents\ChatGPT\blackjack\backups\Hakimi_Blackjack_Lab-before-20260910-151012.zip`。

- 原目录备份 SHA256：`7ed679da13d0534bb0d991ae8a16b4d8b107ce0e38fcb68625955eb3f5c863f0`
- 用户数据库修改前后 SHA256 相同：`ad3082b9411612de5c121681b29dc61d11833487683677d2f87c708107b7ef0b`
- 源文件清单 SHA256：`06640d36065d6c3da42b8af8a755eec55dff658e41e42dc545bcb9a5bd1bf12e`

[final-receipt.json](review-20260910/final-receipt.json) 记录运行环境、实际命令、输出哈希和退出码；[source-manifest.json](review-20260910/source-manifest.json) 对代码、测试、验收脚本、启动文件和依赖说明逐文件列出 SHA256，检查期间这些文件没有变化。

## 交付范围

本次闭环可从 6/7/8 副建靴、选座位、手动录牌、暗牌揭示、分牌/加倍/停牌/投降记录，走到结算或明确未结算结束、追加纠错、保存恢复和完整 JSON/CSV 导出。历史时点查询不使用后续揭示或纠错。运行操作见 [README](../README.md)。

概率/EV、识别与捕获、每轮重置模型、规则纠错分支和仅原注损失等追加注结算仍未支持，能力矩阵已明确标注。修改规则当前采用新建牌靴。未验证所有 DPI、所有最大分牌布局和非 Windows 环境。以上结论是所列功能的本地工程验收，不是未实施阶段的能力声明。
