# 界面布局复核过程

本文件从本次工具输出和截图观察整理，区别于自动产生的最终回执。

1. 初始 1360×900 / 1180×760 的检查只统计已映射控件，因此错误地报告 outside=[]；实际查看发现底部操作区和状态文字被挤出。1180×760 原失败截图保留。
2. 改用固定顶部/底部并压缩左侧后，1180×800 第一次检查仍返回退出1，unmapped 包含 `.!frame.!labelframe3.!labelframe3.!label`（右侧分析边界说明）。
3. 右侧也改为固定动作和说明、组成区伸缩，加入未映射控件检查。最终两尺寸 outside=[]、unmapped=[]，退出0；最终截图已实际查看。

最终 screenshot 和 ui-layout.json 对应最终验收源文件；它们不能作为初次失败时的截图证据。初次失败和中间结果在这里单独说明。
