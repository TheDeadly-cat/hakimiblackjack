# 识别核心对照：首轮结果与剩余工作

按本轮要求保留了现有捕获、人工复核、账本和计时工具，并执行冻结基线、原生 RGB 分类器、YOLO11n 成熟检测框架三条路线。两条新路线在首轮真实开发视频中均未通过默认替代；独立时序实验也没有提高整体识别覆盖。

| 独立成对实验 | 本轮基线正确稳定 | 候选正确稳定 | 结果 |
| --- | ---: | ---: | --- |
| [精确 RGB 分类器](NATIVE_RGB_CLASSIFIER_20260914.md) | 18/31 | 3/31 | 拒识增多且有屏幕数字误收，不采用本轮权重 |
| [YOLO11n 牌角检测](YOLO11_CORE_COMPARISON_20260914.md) | 17/31 | 14/31 | 成功事件延迟较短，但整体覆盖下降、处理开销增大，不采用本轮权重 |
| [持续观察时序策略](TEMPORAL_PERSISTENCE_COMPARISON_20260914.md) | 18/31 | 17/31 | 固定识别记录重放仅改善部分 2 秒期限结果，保持显式实验 |

这些是同一已用于开发的 16:33 录像前 92 秒、31 个新可读参考事件的不同实际运行。帧采样受调度影响，不能把基线 17/18 之间的差异称作模型改变，也不能挑选有利轮次当统一基线。全输出错误预算、连续物理身份及手牌归属尚未验收。P95 只对成功事件计算，全部事件仍保留在期限分母。

## 此前已进行的检测数据扩展

在本轮转向成熟框架前，提交 `99ae41f` 已为旧检测器增加多训练来源与重复来源/留出相交检查。实际查看原 20:21 的 189 个人审上下文，另行标记 8 个下角：人审牌级未改，这 8 个方向决定来自助手而非新增人审。旧 20:03 为 190 正例/176 负例，扩展 20:21 为 181/8，合计 371/184、45 帧、两个文件源。该旧方案扩展模型已经训练，尚未进行新的原生分块或实时替代验收，未升为默认。

固定 1200 步、seed 913、门槛 0.35，产物 SHA256 `f6dcf4d937179b364764e2377ba41d5a0cf04510876224778fa5e0b82612aa36`，本地目录 `.local-evidence/detector-expanded-review-20260914/`。内部整图验证匹配 141/153 只是部分定位诊断，不能当实时识牌结果。20:21 进入该模型和 YOLO 训练后，其原有验证身份不能继续用于这些模型；旧报告仍保留当时的旧划分。

## 仍需完成

首轮完整小图的规模有限，检测边界及代表性条件仍需扩大覆盖；后续应使用可审查的完整目标/排除标注，在相同错误预算下比较 1/2 秒覆盖、漏识、额外误收及纠错工作量。现有原视频继续可用，不能由当前成绩推断必须先录更多素材。

最终验证必须按独立会话预先隔离，不能把 16:33 的另一时间段包装成盲测。本轮已询问哪些既有文件来自另一次开桌/换靴，尚未取得会话身份确认；文件时间间隔和不同 SHA 不自动证明会话独立。当前没有通过独立会话、最新候选 WGC 输入、全局物理身份、手牌归属或默认替换验收。

原始 559 条人审及有重叠的 41 条补充确认保持原记录，不能相加成 600 个独立样本。花色仍作为可选后续能力，未将边注算法加入本轮主线。默认模型、启动器与人工账本流程保持原状态，未合并 main。
