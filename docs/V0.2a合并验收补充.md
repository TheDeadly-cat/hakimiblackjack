# V0.2a 原审查证据补齐与合并验收

2026-09-10。原证据包到达后，**原5项数学测试、48个补充场景、4项交接用例均在当前完整 Windows 工程执行通过**。没有修改原脚本或断言，也没有加载证据包中仅含数学文件的隔离包来代替产品工程。本次不再修改 R1／R2／R3 运行代码；应用仍为 `0.2.0a2`。

## 来源和不可变材料

用户提供 `Hakimi_Blackjack_V02a_增量审查证据包_20260910.zip`，SHA256：`fdc23029c392b1982a0dfea0944a46f7434069cde7375e7ce9573e6b774a2edc`。包内18项清单逐文件校验通过，原始19个文件（含清单本身）完整保留在 [原包目录](acceptance/original-review-20260910-211851-15ebd3/import-receipt.json) 对应目录中。

原材料记录的是 `ee485dd` 上的审查与 Linux 隔离执行，交接用例当时仅做语法检查。这些旧回执没有改写成当前结果。本次新执行输出另存；首次接入时的未提交验证也单独保留，不作为固定提交验收冒充使用。

`review_tests/` 中两个原 Python 文件逐字节保留，附原48场景结果与来源清单。当前生产概率、动作模块、Fraction参考和原数学测试四个文件均与包内原文件 SHA256／Git blob 一致。`scripts/verify_review_handoff.py` 校验这些原始输入的摘要，显式核对运行中的生产模块与参考模块路径属于当前工程，原48脚本的输出写入新的独立目录。

## 固定提交执行

验收提交：`202be3c26ba7429e3e9e2b95a592fe90398b6703`，执行前工作树干净。完整验收 [receipt.json](acceptance/acceptance-v02a-20260910-212132-ec9420/receipt.json) 记录实际命令、退出码、环境及源码清单；检查期间源文件字节不变。后续报告提交不改签这份回执，逐文件核对其运行代码摘要一致。

| 门禁 | 实际证据 |
|---|---|
| 原有及修复回归 | 完整177项 unittest通过，原154项继续执行，新增23项；无删除测试、跳过或 expectedFailure |
| 断网工作流 | 上述测试中46项重叠复测通过，包括跨轮观察、当前分析生命周期、真实窗口恢复与历史复算；0次 socket 网络尝试 |
| 原数学5项 | 原测试方法再次通过，与主测试集重叠，不重复算作新增覆盖 |
| 原补充48场景 | 48/48通过；输入牌、玩家手牌、庄家明牌、检查条件及顺序与原结果文件相同；最大绝对误差 `2.220446049250313e-16`，原阈值仍为1e-10 |
| 原交接4项 | 原文件4/4通过，内部断言也未调整；本次可直接执行的证据取代此前仅按正文重建用例的限制 |
| 自检／编译／实际窗口 | 均退出0；包含观察默认未知、跨轮阻断、已提交但重绘失败时清空旧结果、取消自动后不重启的真实截图 |
| 概率与性能边界 | 原有限研究模板和独立进程5秒预算保持；同一18场景性能和固定种子模拟完整输出随回执保存，不把模拟或小状态覆盖扩张成所有规则保证 |

原包执行的 [独立回执](acceptance/acceptance-v02a-20260910-212132-ec9420/original-review-handoff/receipt.json)、[交接测试输出](acceptance/acceptance-v02a-20260910-212132-ec9420/original-review-handoff/original-handoff.txt) 和 [48场景新结果](acceptance/acceptance-v02a-20260910-212132-ec9420/original-review-handoff/supplementary/supplementary-math-rerun.json) 可逐项复核。48是场景数，不是48个新增 upstream unittest。

## 四项原提案与正式修复的对应

| 原交接方法 | 保留的直接断言 | 当前额外覆盖 |
|---|---|---|
| `test_missing_initial_card_in_unsettled_round_blocks_next_round_analysis` | 第一轮漏初始牌、未结算结束，第二轮完整录入后仍拒绝精确输入 | 明确完整未结算正向对照、旧缺字段、恢复／JSON／CSV／重放／撤销／纠错和真实选择框 |
| `test_committed_input_invalidates_inflight_request_even_if_render_fails` | 新牌只提交一次、提示已保存、旧进程立即取消、无当前旧结果 | 通知丢失时按真实版本阻止晚发布、目标／会话／导入变更 |
| `test_committed_input_removes_displayed_result_even_if_render_fails` | 当前事件前缀增加，已显示结果立即清空 | refresh_all及早期两个刷新步骤故障、恢复后按新前缀计算、保存重试校验 |
| `test_cancel_clears_scheduled_auto_request` | 取消后等待原延迟窗口，原计算入口不得被调用 | 关闭自动、失败刷新后取消、同输入不重启、新输入／显式操作可计算、合法历史复算 |

复现命令：

```powershell
python scripts/verify_review_handoff.py
python scripts/verify_release.py
```

前者包含原5／48／4检查；后者还含完整177项、本机断网复测、性能和截图（截图依赖 requirements-qa.txt）。每次使用唯一输出目录。Windows CI 新增原包验证步骤，在最终 PR HEAD 上重新运行，不能沿用 `039900e` 或 `ee485dd` 的成功记录。

本地材料门禁已补齐。合并操作仍须核对最终 HEAD 的真实 CI 和 PR 状态；GitHub 合并提交及 main 的 CI 是后续独立证据，不写进旧回执。测试均使用自建数据与临时数据库，原桌面记录未被写入或发布。

V0.2b 按既定顺序进入下一阶段：先单玩家分牌，明确再分／分A／DAS／原注与追加注口径，所有手共享剩余牌靴与庄家条件信息；之后再明确多玩家出牌顺序和其他玩家行为假设。本次没有提前移除单玩家限制或新增分牌 EV。
