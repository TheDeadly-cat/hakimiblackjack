# 实时录像到人工核对

主程序的实时录像预览新增「冻结识别帧并核对」。点击后停止该次播放，将已显示候选对应的原始识别帧交给现有人工核对页：原图、选中位置、可横向滚动的裁片，以及新牌／揭示／纠错操作。保存快照不会扣牌，操作和座位必须明确选择后才能确认。

## 使用入口

在项目目录使用已安装视觉依赖的 Python 启动主程序：

```powershell
& '.\.local-evidence\rgb-corner-prototype\venv\Scripts\python.exe' -m blackjack_lab.main
```

1. 主程序打开「识牌核对」，选择本地样式、对应训练模型和录像。需要正式记录时，先在主程序开靴、开轮，再绑定当前轮。
2. 点击「1倍速实时预览」。候选与短时稳定值仍自动显示，无需逐帧确认。
3. 点击「冻结识别帧并核对」，返回静态核对页。该页显示确切原录像帧号和媒体时刻；冻结的是候选对应的识别输入，播放器可能已经显示更新的画面。
4. 点击裁片或选择观察 ID，原图同步标出位置。向右滚动可检查末尾候选。人工选择新牌、揭示或纠正已有事件、座位与牌面，再确认入账。选换候选会刷新牌面提示、清除旧纠错目标；未知归属保持空白。

此入口位于主程序的「识牌核对」页。独立的 `scripts/realtime_preview.py`／`Start_Realtime_Preview.cmd` 演示窗口没有绑定人工核对页，不显示这个按钮。当前桥接支持已打开的本地录像；WGC 到正式人工核对的入口尚未接通。

## 数据与生命周期

- 录像生产者将确切解码帧号写入可选 `source_frame_index`；接帧队列序号和 WGC 媒体时钟不能当成原录像帧号。WGC 不填这个字段，旧记录不补造。
- 只接受当前会话保留的识别结果，检查来源摘要、媒体时刻、模型、区域及图像身份。画面选择使用已显示结果，不使用后台刚完成、用户还未看到的结果。
- 深拷贝原始模型输出和该帧像素，不重新推理，不用历史稳定牌级改写本帧候选。源图与裁片在后台保存；等原推理线程结束后再导入核对页。
- 预览的临时 ID 不写入账本，核对沿用原人工 `FrameTracker` 与 `VisionReviewSession`。原确认记录保留；相同确认请求保持幂等，两张同牌面候选仍分别处理。既有稀疏帧关联仍可能不准确，不能把此桥接称为完整物理身份验收。
- 预览启动后如果改了录像、模型、区域或轮次，旧入口拒绝导入；保存期间原核对结果或账本版本改变，也不覆盖当前工作。
- 每次显式冻结保存一个独立证据目录，已有目录不覆盖。源图、裁片、原模型输出、人工核对 ID、来源时钟、请求时的轮次及摘要可追查。没有逐帧落盘，也没有新采集器。
- 窗口销毁时取消它持有的定时回调，并停止预览；核对页关闭时取消待导入回调并释放本地录像。

证据保存在 SQLite 文件旁的 `.vision/<视频摘要前16位>/realtime/<快照ID>/`。`handoff.json` 保留原始识别与转交身份，人工确认凭据仍使用现有证据目录下的 `confirmations/`。快照文件存在不表示已经人审或已经入账。

## 实机与检查

本地开发录像 `Desktop 2026.09.12 - 21.12.26.07.mp4`，原生识别裁区 1850×520，旧模型和阈值未改。两次均用真实 1 倍速 Tk 播放，并在隔离的临时数据库中以自动化按钮操作冻结。没有人工确认或训练标签变更，也没有操作用户正式账本。

| 运行 | 运行代码 | 冻结原帧 / 已处理结果 | 导入候选 | 图像核对与关闭 |
| --- | --- | --- | ---: | --- |
| actual-1 | `9d02d7271b6627fca0aa50ad54f9907393ee8788` | 2527 / 53 | 15 | 保存图、冻结图、独立重新解码的像素一致；来源与推理结束 |
| actual-2 | `df9d257ff2835e8ea4575bce63f73cab39482e01` | 2532 / 54 | 15 | 同样逐像素一致；来源与推理结束；最后一个候选可滚动到并在原图定位 |

两次临时账本版本均保持 2→2，错误为空，进程退出 0。首次截图发现裁片总宽 2684 px 超过 1384 px 可见区域且没有滚动入口；该图保留，随后增加原图与滚动栏。第二次两张实际窗口截图已查看，最后候选的横向可见区为约 0.2735–1.0。源图在核对页的缩放仅用于查看，没有回送模型重新识别。

两份原 `run.json` 的 SHA256 分别为 `6db637ab50939cfb81747501ffc7b36cba2189f8ce99a5935e6b209225245c40`、`a2d503afa6104c4813f2734a86359b99414caa9bf2f0d144fec1002ddece84dd`。原命令驱动、冻结数据、临时数据库及实际截图在 `.local-evidence/manual-handoff-20260914/actual-{1,2}/`；它们绑定各自运行代码，后续文档不重签。

新增 5 项检查覆盖选中帧、确切原帧号、原预测不变、来源／模型撤回、临时账本不自动变化、重复确认和两张 8，以及 Tk 按钮选择。30 项捕获检查通过；修改后 46 项相关组合检查通过。组合检查前两次曾出现 Tk 变量在工作线程析构的警告及异步等待超时；修正测试窗口在所属线程的清理，并取消实际窗口遗留定时回调后通过。组合末尾仍有一条旧 Tk `ThemeChanged` 清理警告，五项新检查单独运行及两次实机执行没有该错误；不隐去先前失败。

本次完成的是本地录像的显式人工核对衔接。模型错认、下角误收、未完整验证的物理身份／归属、变化区局部检测和 WGC 人工入口仍是未完成项。[原事件准确性结果](REALTIME_EVENT_MATRIX_20260913.md)不因此改善，不能宣称可靠秒级识牌或自动入账已经验收。
