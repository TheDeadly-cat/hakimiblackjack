# 新录像的实际 1 倍速事件对照

当前 RGB 检测候选没有通过替代旧提取器的门槛。八轮真实 Windows Tk 回放已经完成；候选显示更快没有转化为更高的正确稳定覆盖。本报告保留失败结果，不在该保留录像上重训、降门槛或另换架构。

## 运行与参考身份

实际运行代码为 `7b80f8925dc451e605883c8c9aa20137182742fe`，时序策略仍为 `preview-motion-rgb-expiry-2`。此次代码只补齐界面实际绘制状态及过期重绘记录，提供原生尺寸显示与播放结束后关闭选项；原推理权重、检测／分类门槛及关联策略未变。

录像是原先整段保留的 `Desktop 2026.09.13 - 16.33.19.08.mp4`，SHA256 为 `0db7c6933acb62723c40bcc626b84f0a3e161c9be58429bb964bc3d1dbef5cfa`。最初固定前 90 秒，在读取模型输出前增加 2 秒观察尾段，原因是最后一个事件的首次可读区间为 88–88.5 秒，需要完整两秒响应窗口；事件集合仍只取原前 90 秒。

源 ROI 为原生 1850×520，窗口为 `2000x1120+20+20`。八轮记录的显示缩放均为 1.0，实际显示跨度为 91.884–92.133 秒，最后源时刻均约 91.99962 秒。没有以离线慢算或加速回放替代真实窗口。模型各轮加载、预热后驻留，原帧生产端约 20 FPS，识别目标分别为 5／8／10／15 FPS；按最新帧跳过队列中的旧帧。

冻结参考为 `event-reference-frozen-1.json`，SHA256 `c3a98f188f04ed3eca46c2438a52567cbd29298efb3b7ebe27a45eb4330071a6`。包含 36 个助手从原图读取的牌级事件：5 个开场已存在目标、31 个新可读事件。此前 35 个草稿漏计的庄家暗牌已通过原图确认：约 61.25 秒翻出正向 9。原草稿保留，未把该补充当成用户审核。

参考先于模型输出冻结。牌级及上角框来自原图；每 0.25 秒的位置使用人工框模板在源图中的有限范围传播，并单独检查起点条带及明显移动。低相似度、18.25–20 秒部分移牌和 64 秒后第一轮收牌阶段保持未判定。首次可读采用区间，部分遮挡起点的区间宽度达 0.75 秒，不能称逐帧精确真值。

## 31 个新可读事件的结果

下表为当前可判参考锚点上已验证的计数。所有 31 个目标保留在分母中。未匹配和未稳定不从分母删除；未判定位置可能使计数低估，不能把下界解释为完整召回率或精确失败数。

| 方案 | 目标 FPS | 实际处理帧／秒 | 已验证正确稳定事件 | 1 秒内正确稳定下界 | 2 秒内正确稳定下界 | 取帧至候选显示 P95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 旧提取器 | 5 | 4.75 | 12/31 | 3/31 | 10/31 | 319 ms |
| RGB | 5 | 4.77 | 9/31 | 3/31 | 5/31 | 168 ms |
| 旧提取器 | 8 | 7.02 | 12/31 | 8/31 | 10/31 | 317 ms |
| RGB | 8 | 7.28 | 9/31 | 3/31 | 6/31 | 151 ms |
| 旧提取器 | 10 | 5.38 | 11/31 | 5/31 | 9/31 | 571 ms |
| RGB | 10 | 8.64 | 9/31 | 3/31 | 6/31 | 231 ms |
| 旧提取器 | 15 | 10.97 | 12/31 | 7/31 | 11/31 | 285 ms |
| RGB | 15 | 13.04 | 9/31 | 4/31 | 7/31 | 144 ms |

0.5 秒内正确稳定的已验证下界均为 0/31；这不代表起点区间内的所有可能成功均已排除。8 FPS 旧方案的 1 秒计数随首次可读区间取值为 8–10/31，RGB 为 3–6/31；2 秒分别为 10/31 和 6–7/31。这个范围只传播起点区间的不确定性，并非包含所有位置缺口的完整统计置信区间。

8 FPS 旧方案在可判锚点找到 24/31 个候选，验证到 13/31 个正确候选、12/31 个正确稳定结果；RGB 分别为 13/31、10/31、9/31。两者各有 2 个事件曾出现错误候选、1 个事件曾出现错误稳定值。旧方案的首次接受错误有 2 个、首次稳定错误有 1 个；RGB 首次接受错误有 2 个，首次稳定错误为 0，但之后出现过错误稳定并改正，不能只保留最后改对的结果。

开场 5 个目标单列：8 FPS 旧方案在 1 秒内验证到 3 个正确稳定结果，RGB 为 0；RGB 后来在其中 1 个目标上出现正确稳定结果。不得为开场已有牌虚构历史发牌时刻。

为方便核对完整分母，8 FPS 的 31 个新事件按可判锚点结果分组如下；每个事件只进入一组。“曾正确稳定”仍可能包含更早或更晚的错误，上文错误计数另行保留。“未匹配”也不等于已经证实在所有未判位置都漏识。

| 可判锚点结果 | 旧方案 | RGB |
| --- | ---: | ---: |
| 曾正确稳定 | 12 | 9 |
| 只有错误稳定 | 1 | 0 |
| 曾接受牌级，但未正确稳定 | 2 | 2 |
| 仅未知牌级候选 | 9 | 2 |
| 没有匹配到候选 | 7 | 18 |
| 合计 | 31 | 31 |

8 FPS 从可读区间到首个已验证候选的条件 P95：旧方案约 0.653–1.067 秒，RGB 约 22.202–22.568 秒。从可读区间到首个已验证正确稳定结果的条件 P95：旧方案约 2.936–3.556 秒，RGB 约 33.194–33.555 秒。这些分位数仅描述观察到相应结果的事件，完整分母同时保留在期限计数中；未判定位置内可能存在更早结果，因此不能称精确的首次结果延迟。当前不能宣称首候选 P95≤0.5 秒或稳定正确 P95≤1 秒已经通过。

阶段耗时各自统计，未相加构造总 P95。8 FPS 检测／分类中位数：旧方案约 28／21 ms，RGB 约 14／5 ms。第 10 秒实际输出叠图中，旧方案有 17 个角标观察，RGB 仅有 2 个；更低分类耗时也反映了更少目标进入后续处理，不能单独归功于硬件或优化。10 FPS 旧方案这一轮明显较慢，保留其首次实测，不删除、不以其他 FPS 替代；未采集到足以归因的系统负载证据，不据此声称性能随 FPS 单调变化。

## 身份、未判输出与未完成项

8 FPS 可判锚点中未验证到同一个预览 ID 串到两个参考目标，但旧／RGB 分别有 9／7 个参考目标出现多个预览 ID。覆盖不同且仍有收牌、移动缺口，不能称 RGB 身份更可靠或“没有串 ID”。同时产生的重复参考匹配、全部未匹配轨迹状态、错误首发和后续改正分别保留在逐事件 JSON 中；未匹配输出没有自动标成假阳性。

当前布局仍是 `unassigned`，未输出可信手牌／座位归属。正式账本没有由预览自动写入；没有执行正式人工确认，因此不把 0 次自动写入当成“自动记账零错误”。启动器未发出复核／确认操作，但没有监测用户物理点击，人工点击数量为未知，不能补写为 0。

另保留了一次将定位参考框直接送入 `manual_glyph` 的诊断：36 框得到 2 正确、1 错误、33 拒识。这里的框用于空间匹配，包含留边、牌边或微小印刷，未逐项验成训练分布中的紧裁字符；该数字不能作为分类器上限，也不能据此决定训练 CNN。此前已审紧裁框的分类对照仍保留原证据。

本轮完成了八轮实际窗口和首批固定事件评分。9 月 14 日另完成当前路线的真实 A/A、双 8、7/K、小窗口检查及旧／新 WGC 全长识别运行，见 [实机采集与当前回归](WGC_AND_CURRENT_REGRESSIONS_20260914.md)；随后补充了 [WGC 逐事件时钟与绘制区间](WGC_EVENT_TIMING_20260914.md)。本报告的运行身份和结果没有改写，评分重构后逐字段复算仍一致。另已补齐 [首次可见等待区间](SOURCE_VISIBILITY_WAIT_20260914.md)，并将原参考一处误标的 7 勘误为 J，八轮分组指标复算不变；勘误文件与原冻结参考分别保存。完整全输出真伪、物理身份／归属仍有缺口，不能据此宣布可靠秒级识牌或开放自动入账。

## 复现与检查

Windows 使用独立 Python 3.12.14、torch 2.11.0+cu128、torchvision 0.26.0+cu128、RTX 5060、CUDA 12.8；点数模型 SHA256 仍为 `b9fc805cf9a50aa8159f0718bcdcdee49722be7996a277537e1107adaf034be2`，RGB 权重仍为 `4e25bef9b2ff536e19be932201f084dffaa62cebbb2393d7a40fb737cf43d623`。环境及初始化权重许可边界见 [RGB 原型记录](RGB_CORNER_PROTOTYPE_20260913.md)。

各轮完整 Windows 参数数组、PID、退出码和产物 SHA256 位于忽略目录 `.local-evidence/realtime-r3-new-source/matrix-1/*/receipt.json`。入口为 `scripts/realtime_preview.py --video … --style … --model … --fps 8 --first-frame 0 --last-frame 11040 --evidence-limit 4096 --geometry 2000x1120+20+20 --close-on-finish --output …`；RGB 另加 `--detector … --device cuda`。

评分入口为 `scripts/evaluate_realtime_events.py --run … --reference … --output …`。它核对源摘要、完整时长、原生显示比例和缓冲未溢出，空间一对一匹配不使用预测牌级；不完整证据报错，拒绝把未判定位置自动计为假阳性。

本机新增的绘制记录与事件评分共 9 项定向检查通过，另有 30 项捕获契约检查通过。包含手算的完整分母与时钟例子、错牌不影响空间匹配、跨目标复用 ID、未判位置及证据缺失边界。私有原图、视频、标签、模型与逐事件参考保持本地；CI 与真实事件验证分别报告。
