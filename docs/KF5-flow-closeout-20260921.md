# K-F5 手动跟牌流程收口

后续用户要求由Computer Use接手，真实窗口两轮及重启核对已完成，并发现一处撤销提示残留后修复。详情与证据边界见[Computer Use接手核对](KF5-computeruse-20260921.md)。下文自动演练与最初真人计划保留原时间范围。

基线：79f4793f5cb73a716eca5cc2230a547f87584a41；PR #14 → main。此项接续已完成的K-F1～K-F4，保留原计划v2与数值引擎。

## 已核对的起点

79f4793的两次远程Windows检查35523614407、35523611916均已完成并成功：475项，473项执行成功，2项因原336性能材料缺失跳过。后续编译、种子模拟、原交接与N1步骤也实际成功。附件“仍在运行”是较早的审查快照。

原附件3项Windows提案在此基线实跑：三玩家逐键场景通过；最后玩家爆牌与最后DAS牌切庄家两项失败。完整日志保存在独立验收目录，未改写原附件。

## 修改

- `_note_shown()`在提交后的计划已进入`dealer_phase`时也同步界面，覆盖最后玩家爆牌、最后分手21、DAS唯一牌。正常停牌、H1→H2、玩家1→玩家2仍按原计划运行。
- 状态栏使用这次实际保存的座位，不把更新后的“下一目标庄家”写成刚收到玩家补牌的对象。提示、录入座位和实际hand_id分别核对。
- 真实窗口检查发现1360×900默认尺寸下左栏底部提示被裁切：标签需要123像素高度，实际仅59像素。“下一张给谁”与录入目标改放到中部牌桌上方，保留完整文字。规则与录牌按钮语义未变。
- 未修改C#公式、剪枝、容差、预算、SQLite schema2、旧same_rank规则、SINGLE_PLAYER_ONLY；未整合PR #13。

## Windows回归与持续自动演练

`tests/test_kf5_followthrough.py`包含附件3项及新增覆盖，共8项：最后普通爆牌、最后分手21、最后DAS唯一牌、正常停牌、首玩家转下一玩家、撤销与重启、真实八键顺序、默认/最小尺寸的完整提示及祖先边界检查。

本地逻辑修复后的完整套件482项通过（2项材料缺失跳过）；布局调整后的8项集中检查通过。最终提交的完整CI以PR #14当前HEAD为准，新增布局用例后完整套件为483项，不能将运行中写成已通过。

```powershell
python -m unittest tests.test_kf5_followthrough -v
python -m unittest tests.test_pr14_reliability -v
python -m unittest tests.test_same_value_acceptance -v
python -m unittest discover -s tests -v
python -m compileall -q blackjack_lab tests scripts
python scripts/verify_review_handoff.py
python scripts/verify_kf5_flow.py --output .local-evidence/kf5-<新的唯一目录>
```

持续自动演练使用临时、自建模拟器数据和真实Tk键盘事件，设置/模式/结束轮次通过App回调：

- 13轮1/3/5座位跟牌与一轮同点值分牌/DAS，共14轮完成，第15轮启动后退出恢复。
- 111张有效新可见牌、14次原暗牌揭示、188次按键操作。
- 一次有意误录后撤销，一次暂停后的有意拒绝；最终成功演练无意外错误、漏录、重复、错位或中断。
- 自动纠错动作耗时约0.127秒，仅表示脚本执行时间，不能当作真人纠错速度。
- 早期自动演练有合成键未进入Tk的失败记录：原始按下/释放观察均为空，账本无新事件。脚本增加发送前的焦点就绪检查并排队派发，保留原断言与失败日志，不重发不确定输入。
- 保存真实原生分析快照；复制SQLite、`.analysis`、`.deal_plans`后独立恢复，核对完整事件、计划位置、快照身份与历史规则摘要。

## 实际屏幕与真人记录

当前目标机屏幕2560×1440，Tk约96 DPI（100%缩放）。Computer Use直接观察1360×900真实窗口，保留调整前后截图；调整后阶段、最近保存、下一目标、录入对象与分析对象完整可见。1180×800通过几何和祖先裁切检查；本轮不把几何检查写成该尺寸的截图验收。

自动演练不等于真人跟牌。真人使用独立的`human/practice.db`，按预设两轮操作：三玩家初始牌、暂停恢复、误录撤销、庄家揭示结算；随后单玩家分牌/DAS、下一轮及退出恢复。真人反馈、实际事件、异常与完成范围写入独立回执，不在代码中预填完成。未由真人执行100张时，不勾选“真人100张”或推断长期效率。

## 交付与恢复

本轮沿用本任务已有的仓库更新授权，同步到现有PR #14；main不合并，安装包不发布。只提交源文件、回归与说明，不上传临时数据库、分析快照或用户数据。

新用户数据应同时备份SQLite、`.analysis`和`.deal_plans`；此轮已验证自建临时数据的三份恢复，没有读取或改写正式数据库。旧计划缺失/损坏时仍暂停核对，不按牌数猜归属。

回滚代码应在新分支使用普通revert或回到已知提交的独立目录；不要reset用户工作区，不覆盖原数据库，不改写旧事件。计划v2未发生格式迁移，保留回执及原文件。

A1多人桌上的本人分析仍为下一主线，先制定信息集和独立参考；可从本人为最后决策玩家的范围起步。后序玩家策略与共享牌靴影响未建模前继续明确不支持，不删除SINGLE_PLAYER_ONLY。
