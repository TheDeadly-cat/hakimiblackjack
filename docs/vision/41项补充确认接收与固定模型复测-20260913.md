# 41 项补充确认接收与固定模型复测

用户完成新裁框复核后，读取实际保存的记录：3 页、41 项均已确认，待确认新框为 0。40 项为点数，1 项为非牌。原第 4 页的 `[533,328,42,23]` 从 `6` 改为 `junk`；其余 40 项点数与全部框坐标保持原值。新回执和此前点数回执均保留。

本次保存文件的 SHA256 为 `062d08fa8b968d4beba1ada906635b58f314e834b04e84de7f1f8b8eab448d5a`。接收与复测使用提交 `290f42a22527b399191101043116a827fc27bfa0`。本提交仅更新说明，未修改识牌源码。

## 接收与派生

- 冻结实际补充文件，并按原帧摘要和原候选键核对 41 项对应关系；保留真实复核人、时间和原点数回执。
- 创建新的四段录像审核副本及 bundle：仍为 559 条活动标签，其中包含这 41 项的新框确认。**不是新增 41 张牌或变成 600 条标签。**原四个审核文件字节不变。
- 用现有补框导出入口生成 41 项裁片队列。与先前 623 项输入一起检查，共 664 项：原训练来源 311、验证来源 189、开发留出来源 164。新项全部留在原训练来源；已有框、来源、掩膜内容等跨切分检查未发现重叠。
- 664 是可追溯的样本目录计数，不是新模型的训练完成数量。模型未重训，权重、门槛、旧队列及旧成绩未改。旧物理牌编号仍有同页重复，裁框确认不认证跨帧身份，也没有编造新编号。

## 固定模型的补充裁片评测

使用原 `rank-knn-617ba2adc6f1fc3207362da156274a84a06ce939a4d7b7f53c8bfc8526d3e030`，`k=3`、`min_vote=2`、`min_margin=0.02`、`min_similarity=0.85`、`upright_upper` 均不变。

| 41 项已确认裁片 | 数量 |
|---|---:|
| 可辨认点数 | 40 |
| 识对 | 20 |
| 错认 | 0 |
| 拒识 | 20 |
| 非牌 | 1 |
| 非牌误收 | 0 |
| 缺失或无效输入 | 0 |

这是原训练录像中选出的补充裁片，不能称为独立测试或整桌准确率。没有用复核答案替换模型输出。

## 同三张原图的已声明裁框对照

先运行原图提取、几何及固定分类器，并核对诊断输出与实际 `recognize_loaded` 输出一致，再使用不看标签的 IoU 对应规则匹配 41 个已确认框。被筛除候选的反事实分类不计入运行成绩。

| 40 个点数目标及 1 个非牌目标 | 旧几何 A | 新几何 B |
|---|---:|---:|
| 点数识对 | 15 | 20 |
| 点数错认 | 1 | 1 |
| 点数拒识 | 6 | 9 |
| 点数未匹配 | 18 | 10 |
| 非牌误收 | 0 | 0 |
| 其他未匹配接受输出 | 2 | 2 |

两版保留的同一错认是原第 7 页 `[687,126,30,20]` 的 `4 → A`。新几何多识对 5 个、少遗漏 8 个；仍有拒识、漏框和错认。两个未匹配接受输出缺少完整画面真值，未擅自归为正确或错误。

原帧正式评测入口另执行一次，按预期退出 **2**、`valid=false`，原因包括局部裁框范围、缺少整帧确认及重复物理牌编号。三帧在该入口被判为无效输入；其中候选数为 0 **不代表真实检测结果为空**。上表来自单独的真实管线与事后裁框对应诊断；完整 precision/recall 和时间指标继续留空。

## 本机证据与剩余范围

证据目录：`.local-evidence/supplement-confirmed-20260913-112820/`。包含接收回执、逐项修正、实际保存快照、新审核 bundle、41 项裁片队列、输入切分检查、裁片预测、原图 A/B 全量轨迹、范围检查及各实际命令/退出码。私有图像和标签不上传仓库。

这 41 项补充确认已完成接收。独立录屏、少量完整正例画面，以及连续牌张身份/事件验收仍需相应材料。本次未重跑 WGC、训练模型或修改分类门槛；此前 WGC 和 CI 证据继续绑定各自执行提交。
