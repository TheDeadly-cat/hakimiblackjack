# 已确认空桌负例与新框续审

在复核阶段 C 的缺口时，从已有记录中找到一张用户完整确认的空桌图：20:03 录屏原第 1 页，`card-00000000.png`，原 `complete=true`、复核人和时间均保留。只复制该帧记录作为对照，没有创建新的人工确认。

固定已交付点数模型及门槛，分别使用几何 A (`upper-upright-corner-1`) 和 B (`upper-upright-local-2`) 运行现有原帧评测：

| 同一张人审空桌图 | A | B |
|---|---:|---:|
| 已标可见点数目标 | 0 | 0 |
| 分类候选 | 3 | 3 |
| 接受点数输出 | 1 | 1 |
| 未匹配接受输出 | 1 | 1 |
| 报告完整性 `valid` | true | true |

两版都将桌面印刷字的一部分误判为 `8`，框为 `[421,248,27,16]`。这证明当前模型/几何仍有背景误收；`valid=true` 和命令退出 0 只表示本次输入与报告完整，**不表示识别通过**。`junk_as_rank=0` 也不能掩盖这一个已知空桌上的误收，因为本页没有逐框的 junk 标注，误收记录在 `unmatched_accepted` 中。

该页面来自训练来源，属于开发负例对照，不能替代独立背景测试；不能从单张空桌图外推出整模型的准确率。没有利用它调模型、改分类门槛或继续添加几何启发式。原有错误分型和备用的小型几何模型对照设计继续保留。

同时修正了一个复核流程缺口：原窗口仅看点数的人审标记，可能把助手重裁后尚未确认的新框跳过。现在活动项只要仍有新框待核，就会进入续审队列，即使旧页已经完成。确认新框时保留此前的人审点数回执，现有其他确认和空页不会被重置。

41 个旧待确认框已整理为 3 页的单独副本，提供桌面快捷方式。副本只保留原字段与新框，未修改原 559 条记录；其旧编号仍有 16 处同页重复，没有编造新的“已验证物理身份”。框确认完成后也不能自动宣称跨帧事件验收。

本机补充证据位于 `.local-evidence/corner-remediation-20260913-013033/continuation-2/`：`confirmed-empty-frame.json`、`empty-A.json`、`empty-B.json`、`empty-false-positive.png`、`commands.json` 及 `new-crop-review/`。旧证据包与成绩不覆盖。本轮没有新的独立视频或用户确认输入，阶段 C 仍未完成。

新增 3 项复核契约测试，验证旧完成标记不能跳过新框、原有完成项不被重置、确认新框保留原点数回执。相关 27 项检查通过；这些是流程验证，不是识牌精度验收。
