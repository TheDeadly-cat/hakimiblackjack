# 真实 A/A 分牌连续检查

在现有 `Desktop 2026.09.12 - 20.21.54.05.mp4` 中找到实际分牌事件：约 608 秒，荷官把 A/A 拆成两手，两张 A 交换了左右位置；约 612 秒右手补入 10，约 614 秒左手补入 6。该事件来自既有素材，无需新录屏。

## 来源与运行

此前仅按 A/A 和 8/8 邻近人审框筛查，遗漏了这副对子，因为原抽样页面只标记了其中一张 A 的上角。此次扩大到所有同点数或同牌值的邻近框，得到 46 个候选页面，逐页查看后，在 20:21 源约 600 秒发现 A/A；随后检查 580–660 秒的 41 张两秒抽样原图，确认实际拆分与补牌。

候选页面和相同牌值只用于寻找事件，没有作为真实分牌的判据，也没有送入识别或关联。原有人审标签不改写，新看到的另一张 A 不冒充旧记录已有的完整标注。

在 `18b3353e0935c57fbdcf9e6a7dc6d7fcda67b000` 上执行 `VideoReader → prepare_style_image → recognize_loaded → FrameTracker`，逐帧运行第 **72,000–73,920 帧**（约 600–616 秒），共 **1,921 帧**，约 471.35 秒完成，零解码/推理异常。脚本退出 0，诊断状态为 `valid=false`。

源 SHA-256 为 `8d62e197762a56f7e9be747baba5475e4fa0118d239148ddd7050be4db396f7f`。使用原 1850×520 牌桌裁区，不缩放原图，点数模型与门槛保持冻结。模型摘要 `cc1677fcceea7a40db51f1a7cbcacb59092f66ecf7f9604a6dbd028f4eb1681b`；跟踪源码摘要 `800ab06d3e9d43956bf346b39bc8d2fea1cfc0d4af546241b73777c0f72a2f61`。没有操作者切局/手牌绑定或账本写入。

## 只评价可见的正向上角

分别保存七个原始锚点，以及 605–615 秒每 0.25 秒一帧的 41 张动作原图。动作预览是抽样展示；运行 trace 覆盖完整 1,921 帧。

事后建立六个可比较锚点、12 次可见正向上角的助手参考。用红心/方块及原图动作区分两张 A 的物理身份，这些内容只用于评估，没有加入花色识别器、轨迹输入或人工确认记录。608 秒双手遮挡的锚点不伪造可见上角框。

右手补 10 后，方块 A 的上角被盖住；左手补 6 后，红心 A 的上角也被盖住。它们剩下的下方 A 只作为排除项，不能补成新的正向识别或连续身份。共另外检查 12 次下角参考。

| 局部参考结果 | 次数 |
|---|---:|
| 正向上角识对 | 3 |
| 正向上角错认 | 0 |
| 有候选但分类拒识 | 6 |
| 未匹配到运行观察 | 3 |
| 下角参考被接受为点数 | 0／12 |

三个未匹配目标都是分牌后的 A 上角：原始提取框存在，但当前几何策略把它们放入 `uncertain`。这仍是局部几何在分牌后连通牌体上的不足，没有临时微调门槛使本例通过。

## 身份与归属结论

分牌前两张 A 分别有稳定、不同的候选 ID，虽然点数拒识；分牌后上角退出观察输出，因此**不能证明两个 ID 成功跨越分牌保持**。参考对应中的零合并、零断裂统计缺少分牌后的 A 观察支撑，不能写成身份验收通过。补入 6 和 10 的候选使用不同 ID；6 在两个末端锚点识对，10 先拒识、后识对。

现有冻结布局只有 `unassigned`，并未自动输出拆分后的左右手归属。助手对左右手的判断不是运行预测或操作者确认，所以归属评测仍不成立。其他座位未完整标注，另有 11 次未匹配本局部参考的接受输出，不把它们全部算成非牌误报。

这次补齐了“实际分牌动作上的运行检查”，结果是开发诊断未通过完整连续验收。点数、身份、归属与账本正确性分别报告；没有用合成分牌测试替代实录像，也没有用下角掩盖漏识。

本机证据在 `.local-evidence/find-split-events-20260913/`，包括来源页面、两秒邻帧、逐帧 trace、动作原图、助手参考、逐阶段对应和 `real-split-preview.gif`。真实材料不上传 GitHub。
