# V0.3a R3 封闭合成集与 Windows 工作流

日期：2026-09-12  
工作目录：`C:\Users\Administrator\Documents\ChatGPT\blackjack\vision-v0.3a`  
HEAD：`cdd0e8f2e34e27f9652575556b3d4b5c9f25603d`（祖先含已合并 main `17c664375da2809138f8290fe0f7a77fc1603d25`）  
证据：`.local-evidence/acceptance-v03a-20260912-124156-212169/`（`receipt.passed=true`）

这是自建 `synthetic-felt-v1` 的离线候选验收，不是通用 OCR，也不是真实牌桌识别。`platform_claim = none`。匹配度不是正确概率。人工纠正不计入模型成绩。

## 冻结规模

种子仍为 `20260912`。计划：每类主画面 55 帧 × 13 点 = 715 可辨认帧，每帧庄家区与玩家1 区各一张；干扰 200 帧（空区 80 + 牌背 60 + 遮挡 40 + 非绿绒面 20）。

实跑：`915` 帧，`1430` 张可辨认实例（每类 ≥91，最低 7 点 91），干扰 `200` 帧。

## 原始预测（人工纠正未计入）

见 [R3-holdout-report.json](R3-holdout-report.json)。

| 门槛 | 要求 | 实值 | 是否过门 |
|---|---|---|---|
| 可辨认实例 | ≥1000 | 1430 | 是 |
| 每类实例 | ≥50 | 最低 91 | 是 |
| 干扰帧 | ≥200 | 200 | 是 |
| 已接受点数准确率 | ≥0.99 | 1.0（0 次错误接受） | 是 |
| 端到端检出率 | 建议 0.95 | 0.8636 | 否（建议门槛，不因此放宽拒识） |

拒识主要来自 JPEG/缩放后的 2/3/5/10。空区与牌背误检接受为 0。未为刷 95% 降低 `ACCEPT_SCORE` / `MIN_MARGIN`。V0.3a 仍要求人工确认后才入账。

识别计时（holdout 批）：`recognize_seconds=17.53`，约 `19.2ms/帧`。入账不计入此数。

## 确认入账（与识别计时分开）

脚本化两张 8：识别 `0.116s`，入账 `0.016s`。两张 8 各扣一次；确认后再拒绝被挡住；前缀事件长度不变。演示 `vision_demo.py` 不写账本。

## Windows 工作流

```
python scripts/verify_vision_v03a.py --rebuild-holdout
```

同次回执：`selfcheck`、识牌/确认测试、T10 实验门禁与 DAS 回执绑定、demo 不入账、holdout 原始预测、`git ls-files` 数据保护（holdout 原图不入库）。未访问用户库。

本机 opt3 336 原材料若不在本工作树，对应两项 skip；空壳 / `EV=999` / 共用 stub 仍拒绝。

## 能力边界

见 [V0.3a能力与不支持.md](V0.3a能力与不支持.md)。未做：真实平台、相机、授权窗口捕获、自动入账、连续帧跟踪、三/六整轮模拟。
