# V0.3a R0+R1 运行记录

日期：2026-09-12  
工作目录：`C:\Users\Administrator\Documents\ChatGPT\blackjack\vision-v0.3a`  
分支：`vision/v0.3a-r0-r1`（从 `origin/main` = `137bcdcd7da6e6ba35ad8033a3e09da60a1aa906` 拉出，未回退更新版本）  
数学线工作副本仍在 `handoff/t9-das-benchmarks` @ `6f3b256`，本轮未改求解器。R2 仅小范围适配控制器/账本入账字段。

## 环境

- Windows，Python 3.14.6
- 可选依赖试装：`opencv-python-headless==4.14.0.94`（numpy 2.4.6 已存在）
- 未把 OpenCV 加入 `blackjack_lab.main` 启动导入路径

## 已交付

| 项 | 位置 |
|---|---|
| 样式与上限 | `blackjack_lab/vision/styles/synthetic_felt_v1.json`，`fixtures/vision/synthetic-v1/STYLE.md` |
| 候选契约与示例 | `blackjack_lab/vision/contracts.py`，`styles/example_candidate.json` |
| 识别管线 | `image_io.py` / `detector.py` / `rank_recognizer.py` / `pipeline.py` / `evidence_store.py` |
| 演示 | `scripts/vision_demo.py`（CLI + `--gui`），不写账本 |
| 评估 | `scripts/vision_benchmarks.py`，`docs/vision/R1-holdout-report.json` |
| 测试 | `tests/test_vision_*.py`（无 OpenCV 时跳过识别器用例） |

## 原始预测（人工纠正未计入）

`synthetic-felt-v1` holdout 种子 `20260912`：310 帧，349 张可辨认牌，50 张干扰帧。

- 非拒识点数准确率：**1.0**（0 次错误接受）
- 可辨认端到端检出率：**0.825**
- 漏检率 0.023，拒识率 0.152
- 空区/牌背误检接受：0

这只证明自建样式。2/3/10 在 JPEG 与缩放后更容易因匹配度不足或前两名接近而拒识，符合“宁待核对、不猜测”。

## 后续

- R2 已交付，见 [R2 入账协议](R2-入账协议.md)
- R3 已交付，见 [R3 验收](R3-验收.md) 与 [R3 holdout 报告](R3-holdout-report.json)
- 仍未做：连续帧跟踪、授权窗口捕获、真实平台、云端 API
