# 正向上角误排：固定基线的逐阶段诊断

基线为 `fc0fa7e8e170a6a942869dc409ef4c5fc488066c`，模型为 `rank-knn-617ba2adc6f1fc3207362da156274a84a06ce939a4d7b7f53c8bfc8526d3e030`。本阶段仅增加诊断；模型、权重、分类门槛、上角规则及用户原标注没有改变。

本机从原有 25 页、163 个已知点数目标重新提取，不根据报告制造样本。完整原帧通过 SHA256 核对，运行入口与诊断入口逐项核对保留框及已接受点数一致。

| 阶段 A 结果 | 数量 |
|---|---:|
| 原始提取候选 | 783 |
| 原几何规则保留 | 265 |
| 原几何规则排除 | 518 |
| 指定的 48 个已知上角全部复现 | 48 |
| 其中判作边缘/方向不确定 | 34 |
| 其中判作下角 | 14 |

另对排除的 48 个真实裁片进行固定分类器的离线反事实推理：18 个可识对、2 个会错认、28 个拒识。该结果没有进入运行候选或账本，不能计入原系统识对数，也不支持直接关闭全部筛选。完整诊断包含其他原始连通块及其尺寸、颜色、面积等提取拒绝原因。

每个原始候选记录来源摘要、帧摘要、框、原始提取结果、上/下距离、筛选决定、模型身份及实际/反事实分类拒绝原因。人工答案只在提取、筛选和分类完成后做几何匹配；诊断识别 API 没有 GT 参数。

可复现入口：

```powershell
python scripts/diagnose_corner_stages.py --cases <cases.json> --model <frozen-model> --layout <layout.json> --focus <existing-missed-diagnostic.json> --output <new-directory>
```

`cases.json` 是含 `session`、`annotations`、`source_index` 的列表。输出拒绝覆盖。原帧不存在、来源不符、图片被修改、生产路径与诊断不同、指定的旧遗漏项无法复现，均明确报错。

本次原文件快照和命令回执位于本机忽略目录 `.local-evidence/corner-remediation-20260913-013033/`。`phase-a/diagnostic.json` 保存全量阶段与对应关系，`phase-a/逐项原因.md` 链接 48 份上下文和按原字节复制的原 PNG。原模型和标签保留字节摘要。相关 2 项测试检查反事实隔离、模型不被改动、生产入口一致、原图复制、缺失目标及内容篡改拒绝；测试不认证真实牌桌正确率。

下一提交仅改善几何规则与待核对路径，保持分类器和门槛不变，旧策略用于 A/B。原 25 页属于开发集；独立片段、少量完整页面及真实 Windows 捕获仍须单独验证。
