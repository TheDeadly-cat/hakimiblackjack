# 连续回放与真实 Windows 捕获：阶段 C 当前证据

阶段 A 的逐项诊断与阶段 B 的有界几何修复已完成。阶段 C 已执行连续解码和真实 WGC 捕获，但尚未取得新的独立片段，也没有完整的原帧/跨帧人审真值，因此整个识牌目标尚未完成正式验证。

本轮继续固定人审模型 `rank-knn-617ba2adc6f1fc3207362da156274a84a06ce939a4d7b7f53c8bfc8526d3e030`，几何为 `upper-upright-local-2`，适配器摘要 `cc1677fcceea7a40db51f1a7cbcacb59092f66ecf7f9604a6dbd028f4eb1681b`，样式 `navy-live-felt-v1`。模型文件、分类门槛和原标签没有变化。

## 已执行的原录像连续处理

选择已有 20:42 录屏的原帧 0–1199，实测帧率约 120.00048 fps，约 10 秒。视频完整 SHA256 为 `80cafcdc288d3d77ea8609c3ff920712c219b7ef692969b3303cf7ea7111f611`，ROI 是已记录的 `[500,800,2350,1320]`；该来源已参与本轮开发，明确不属于独立片段。

现有 `evaluate_video_timeline.py` 顺序解码并识别了全部 1,200 帧，使用共享模型入口和 `FrameTracker`，没有解码或推理错误，没有向识别器传入人工框、点数、身份或局号。没有构造人工轮次绑定，没有写账本。

该区间只有 1 张原有标注帧、6 个已知目标，页面未完整确认。报告如实返回 `valid=false`、`frame_human_review_incomplete`，正式 metrics 留空。单个标注时刻内显示的零身份切换、零合并不证明整个片段身份稳定；同牌重复、两张同点数、分牌移动、遮挡后重现、归属和后揭示仍缺少足够时间真值及操作回执，不能宣布通过。也没有用每 40 秒一页替代逐帧处理。

## 已执行的原生 WGC 捕获

在临时本地播放窗口展示上述录屏 ROI，约 10 fps 展示，窗口本身由真实 Windows Graphics Capture 捕获。播放仅用于测试，原录像只读，没有操作平台页面。播放窗口不是实时牌桌，展示层也没有播放全部 120 fps 原帧；原始全帧处理由前一项单独验证。

先暂停首帧，用真实 WGC 图像匹配原 ROI，实测窗口内裁区为 `[1,31,1850,520]`，归一化写入专用样式；没有根据桌面位置猜裁区。随后通过现有 `live_capture_probe.py --model ... --style ...` 捕获 15 秒。

| 真实捕获回执 | 结果 |
|---|---:|
| 后端 | windows-graphics-capture |
| 运行时长 | 15.026 秒 |
| 到达帧 | 80 |
| 被采样接受 | 28 |
| 进入指定模型 | 27 |
| 候选输出累计 | 464 |
| 接受点数输出累计 | 104 |
| 黑帧 / 队列丢弃 / 尺寸变化 | 0 / 0 / 0 |
| 回调错误 / 拒绝原因 | 无 / 无 |
| 账本写入 | false |

464 和 104 都包含跨帧重复，不能当作物理牌数量或识别准确率。采样目标 3 fps，实得 1.86 fps；WGC 由画面变化驱动，这不是全链路帧率保证。回执中的模型 ID、样式和摘要与阶段 B 对应，采集最终正常停止，临时播放进程已关闭。

## 现有窗口与增量复核

临时数据库中的现有识牌窗口加载真实 PNG 后显示新模型身份、11 个识别观测和 19 个独立几何待核项。真实 WGC 截图确认“几何待核”入口已渲染。临时窗口关闭前后账本牌数均为 0。待核查看和切源撤销的操作由现有 Tk 集成测试覆盖。

桌面自动化辅助工具绑定 Python 窗口时两次返回相同的窗口归属校验错误，已按恢复限制停止界面输入；没有声称完成该辅助工具的点击验收。项目原生 WGC 的上述真实捕获独立执行成功，不能混为同一验证。

另外生成了 21 项增量意见：原 48 项里的 5 个方向范围疑点，以及 16 个未匹配接受输出。原框、旧标签与复核人保持不变，新意见标为助手建议，包含原图联系图和编号，用户可按编号确认，无需重画 559 条标签。它不是已完成的人审真值，也不自动补写整页完成状态。

## 当前未完成项与下一步

- 未取得一段未参与本轮调参的新独立牌桌片段，当前文件夹中已有四段不能改称盲测。
- 21 项增量意见尚待针对性确认，少量整页的完整性与非牌真值尚未获得人审确认。
- 分牌、后揭示、跨帧身份及实际人工操作/账本回执的连续事件验证尚未完成；当前仅报告连续处理和候选轨迹，不将工程测试或成功捕获替代这些证据。

唯一下一阶段是独立片段上的增量真值验证，继续固定本轮开发基线。不要重复训练或增加启发式来掩盖这些缺口。

本机证据目录为 `.local-evidence/corner-remediation-20260913-013033/`：`continuous-development.json`、`wgc-runtime/capture-report.json`、`wgc-runtime/latest-candidates.json`、`wgc-calibration-match.json`、`wgc-scene/scene-report.json`、`增量21项复核意见.md`、`protected-data-check.json` 和 `commands.jsonl`。原始素材、模型、用户标签及这些运行产物只在忽略目录中，未上传 GitHub。
