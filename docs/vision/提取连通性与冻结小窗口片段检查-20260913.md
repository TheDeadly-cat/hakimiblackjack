# 提取连通性与冻结小窗口片段检查

本次在运行源码未改变的 `de3ee0cda49b5d23206761387fa355fb1a3ca534` 上继续检查现有录像。两个新发现发生在上角策略之前：可见笔画与外部背景连通时会漏提取，小窗口中的字符尺寸也低于现有提取范围。仅改上角筛选不能解决这两类问题。

## 7 的提取框消失：已定位到轮廓填充

在 21:12 录像的六个原始帧上，分别保存白色掩膜、闭合后的轮廓、填充牌体和最终字形孔洞。原轨迹框 `[1013,257,27,20]` 只用来事后查数，没有用于生成候选或补点数。

| 检查项 | 第 12,655 帧 | 第 12,656 帧 |
|---|---:|---:|
| 旧框内白色像素 | 306 | 306 |
| 旧框内非白像素 | 234 | 234 |
| 处于填充牌体内的非白像素 | 234 | 6 |
| 最后孔洞掩膜的像素 | 257 | 0 |
| 7 所属轮廓背景是否通到画布外缘 | 否 | 是 |
| 是否产生原 7 候选框 | 是 | 否 |

闭合后，原本约 251 像素的独立背景连通区变为触及画布边缘的大背景连通区。`RETR_EXTERNAL` 的牌体轮廓填充不再覆盖这段笔画；最后开运算把剩余少量像素去掉。原图中的 7 没有因此消失。第 12,740 帧重新形成封闭关系，框也重新出现。

这条证据解释了上一轮 7/K 串用为何无法被“旧位置还有几何待核框”保护：旧位置连提取框都没有，几何模块没有收到它。没有扩大闭运算核或调整墨色、尺寸、分类门槛；也不把一个局部修复冒充完整策略。

## 预先固定的另一段现有录像

固定中午 `Desktop 2026.09.12 - 12.58.11.02.mp4` 的第 **36,000–37,800 帧**（约 300–315 秒），共 **1,801 帧**，检查锚点为 36,000、36,480、36,960、37,440、37,800。计划在本次首次解码前写入，未按新运行的好坏挑选片段。

源 SHA-256 为 `9fead02da5070a9e2bec8cf2aa1b2a7da212b4f52bd924b2f96fcda823146881`。当前点数模型 manifest 的训练来源只有晚间 20:03 源 `3831ddee46d4f00b8bf78e8b8b26eb5feb56d6490850c5ea02bdbd5225356c45`；当前几何 A/B 的原 25 页来自晚间 20:42 和 21:12 源。

但是，中午录像在 V0.3b 早期开发中已有使用记录（文档提及第 49,222 帧），所以不能说它从未被使用。它与晚间素材是否属于不同物理牌靴也未验证。此处只说明与当前权重训练来源、原 25 页调参集合分离，**不宣称完全未见的端到端盲测或最终独立验收**。

## 保留首次裁剪不匹配的结果

首次套用晚间 1850×520 裁剪区域，完整运行约 70 秒。随后原图检查确认中午录像是较小的浏览器窗口；虽然源尺寸同为 2560×1440，旧裁剪区域没有覆盖这张桌子的全部牌。

首次计划、逐帧 trace 和报告全部保留为配置不匹配证据。第二次保持相同帧范围、点数权重和门槛，使用仓库已有 `navy_live_felt_v1.json` 中的源/绒面裁剪，合成源框 `[95,708,1180,282]`；归属统一为 `unassigned`，不把未经校准的座位号当成真值。没有放大图像、重训或修改原文件。

修正后的完整运行约 63 秒。两次都是同一组 1,801 个源帧，不相加成 3,602 帧独立覆盖，也不隐藏首次配置问题。

## 校准后仍无候选：字符尺度不足

修正后每帧都经过真实 `VideoReader → prepare_style_image → recognize_loaded → FrameTracker` 入口，零解码/推理异常。整段输出 0 个观察、0 个接受点数，累计 7 次几何待核；五个固定锚点上的旧/新几何策略均输出 0 个观察。

这里要区分调用层次：**识别入口执行 1,801 帧，但点数 `predict_mask` 调用为 0**。提取/筛选没有给点数模型提供可用字形，不能把这次写成分类器连续识错 1,801 帧。

从第 36,000 帧事后选取四个可核对的正向上角，形成单独的助手参考：

| 上角参考 | 框 `[x,y,w,h]` | 当前提取拒绝理由 |
|---|---|---|
| K | `[502,191,18,11]` | 高度 11，小于 14 像素下限 |
| 5 | `[596,198,12,10]` | 高度 10，小于下限 |
| 5 | `[678,193,14,9]` | 高度 9，小于下限 |
| 4 | `[820,128,15,7]` | 高度 7，面积也不足 |

四个原始连通块都存在，但在尺寸/面积阶段被排除，尚未进入上角策略。另标出两个明确 UI 负例作为局部参考；新旧策略对四个正例均未匹配，对两个负例均未误收。其余牌没有完整标注，因此精度/召回率不计算，`valid=false`。这些框和点数是助手参考，没有创建人工确认或覆盖旧标签。

该段可以验证小窗口输入的适用边界，不能用其零输出证明非牌误报受控，也不能用它证明遮挡、分牌、归属或揭牌身份已经通过。识别范围与实际字符像素尺度必须一并报告。

## 身份、命令和证据

模型权重 SHA-256 保持 `b9fc805cf9a50aa8159f0718bcdcdee49722be7996a277537e1107adaf034be2`，模型 manifest 保持 `f11a2c592054e0df51532d8aecffe8f170736249984d3bea622bc47a289ccbbe`。B 适配器摘要保持 `cc1677fcceea7a40db51f1a7cbcacb59092f66ecf7f9604a6dbd028f4eb1681b`，A 为 `a0340e195ea139e42f199e0aabc9b2f00fd96e8339ca71540d721408215434ad`。

本机实际执行 `probe_body_discontinuity.py`、`run_frozen_earlier_clip.py`、`run_calibrated_clip.py`、`score_partial_reference.py`，退出均为 0。前两次运行报告中的“classified frames”字段代表识别入口经过的帧；`verified-diagnostic.json` 单独记录点数调用为 0，避免混淆。退出 0 表示诊断脚本完成，正式评测状态仍为 `valid=false`。

本机证据分别存于 `.local-evidence/extraction-continuity-20260913/`、`.local-evidence/frozen-existing-clip-20260913-12-58/`、`.local-evidence/frozen-existing-clip-20260913-12-58-calibrated/`。原录像、原图、模型和标签不上传仓库。旧 WGC 证据保持原执行身份，本轮录像检查不冒充新的 WGC 实跑。

下一步按 [错误分型与替代对照设计](几何与提取错误分型及替代对照设计-20260913.md) 执行单独的几何候选实验；不继续向当前规则追加阈值，也不因最终独立验收缺口要求用户先重录或重画全部数据。
