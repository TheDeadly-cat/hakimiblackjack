# navy-live-felt-v1

日期：2026-09-12  
声明：这是**本机旁观录像的固定窗口样式**，不是 Stake / Pragmatic 官方适配，也不是通用 OCR。`platform_claim = none`。

## 输入

- 原录像只读。本仓库不存放原 mp4，也不存放未打码的平台整图。
- 本机证据在 `.local-evidence/`（gitignore）。
- 冻结条件：2560×1440 桌面、浏览器窗口位置与这段 `Desktop 2026.09.12 - 12.58.11.02.mp4` 一致。窗口一挪，先重标 `source_crop`。

## 裁切

1. 全桌面 → 直播画面 `source_crop`
2. 直播画面 → 深色绒面 `felt_crop`（1180×282）
3. 在绒面上标庄家 + 玩家1–7（从左到右，座位号待人工核对）

## 识别上限

- 先定位牌形候选并给出座位提示。
- 绒面印刷字（如 BLACKJACK PAYS）可能被框到，必须人工拒绝。
- 重叠牌按宽度切分，可能切偏。
- 没有本地点数模板时，不接受自动点数，只等人确认。
- 中途开录：不能把剩余牌当成完整新靴。

## 禁止

把账户、余额、聊天、未授权整图写入 git。不绕过受保护内容录屏。
