# Optional local image/video recognition and original-frame annotation.
# Manual recording, SQLite recovery and math self-check remain stdlib-only.
# Install: python -m pip install -r requirements-vision.txt
# WGC capture is separate: python -m pip install -r requirements-capture.txt
#
# Frozen to the actual Windows / Python 3.14 development environment on
# 2026-09-12. Official PyPI binary dependency resolution checked on that date.
# Use exactly one OpenCV distribution: windows-capture depends on opencv-python,
# so installing opencv-python-headless alongside it would overwrite the same cv2.
opencv-python==5.0.0.93
numpy==2.4.6
Pillow==12.2.0
